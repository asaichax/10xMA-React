// PostCSS configuration for 10XMA Creative Production & Media Agency website
// Currently using Bootstrap CSS, so we only need autoprefixer
export default {
  plugins: {
    autoprefixer: {},
  },
}
